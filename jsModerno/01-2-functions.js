function soma(primeiroParametro, ...argumentos){
  console.log({primeiroParametro})
  console.log({argumentos})
}

soma(1, 2, 3, 4, 5, 6, 7, 'Gabriel')

//rest operator é uma forma de criar parâmetros dinamicamente, e ilimitadamente, basta criar um parâmetro com "..." antes do nome.