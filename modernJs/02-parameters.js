function printGreetings(message = 'Mensagem padrão'){
  console.log(message)
}

printGreetings('Seja bem-vindo')

//We are declarating a default result to the argument. When the user, doesn't pass anything as the parameter, the default parameter will be used. And whe he pass, the new parameter will be used.